<Window x:Class="SecureTextEditor.GUI.SaveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SecureTextEditor.GUI"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        mc:Ignorable="d"
        Title="Save Options" Height="360" Width="640" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" >

    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>

        <Label Grid.Column="1" Content="Save Options" HorizontalAlignment="Center"/>

        <!-- Save options -->
        <Grid Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="1.5*" />
            </Grid.RowDefinitions>
            
            <!-- Security option -->
            <StackPanel Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Grid.Column="1" Margin="0 0 0 5" Content="Security Type:" />
                <ComboBox Name="SecurityTypeComboBox" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="200" />
            </StackPanel>

            <!-- Cipher mode option -->
            <StackPanel Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Grid.Column="1" Margin="0 0 0 5" Content="Cipher Mode:" />
                <ComboBox Name="CipherBlockModeComboBox" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="200" />
            </StackPanel>

            <!-- Cipher padding option -->
            <StackPanel Grid.Column="2" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Grid.Column="2" Margin="0 0 0 5" Content="Cipher Padding:" />
                <ComboBox Name="CipherBlockPaddingComboBox" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" Width="200" />
            </StackPanel>
        </Grid>
        
        <!-- Cancel and save button -->
        <Grid Grid.Column="1" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Name="CancelButton" Content="Cancel" Click="CancelSave" />
            <Button Grid.Column="2" Name="SaveButton" Content="Save" Click="Save" />
        </Grid>

        <!-- Waiting indicator -->
        <ContentControl Name="SavingIndicator" Grid.Column="2" Grid.Row="2" HorizontalAlignment="Right" Focusable="False"
                        Visibility="Hidden"
                        ContentTemplate="{DynamicResource {x:Static adonisUi:Templates.LoadingCircle}}"
                        Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}" />
    </Grid>
</Window>
